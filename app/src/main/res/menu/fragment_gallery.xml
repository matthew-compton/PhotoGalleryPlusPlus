<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_item_gallery_search"
        android:icon="@android:drawable/ic_menu_search"
        android:title="@string/search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="ifRoom"
        />

    <item
        android:id="@+id/menu_item_gallery_clear"
        android:icon="@android:drawable/ic_menu_close_clear_cancel"
        android:title="@string/clear_search"
        app:showAsAction="ifRoom"
        />

    <item
        android:id="@+id/menu_item_gallery_history"
        android:title="@string/search_history"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/menu_item_gallery_polling"
        android:title="@string/polling_start"
        app:showAsAction="never"
        />

</menu>
